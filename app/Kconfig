# Define the custom heartbeat period option
config APP_HEARTBEAT_PERIOD_MS
    int "App Heartbeat Period in ms (100-2000)"
    range 100 2000
    default 500
    help
      Sets the sleep duration for the application heartbeat LED toggle.

# Source the main Zephyr Kconfig file
source "Kconfig.zephyr"